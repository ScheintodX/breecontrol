"use strict";

var H = require( '../helpers.js' );

module.exports = function( base ) {

	return {
		base: base,

		var run: function( emit ) {


		},

		var msg: function( emit, topic, data ) {


		}

	}

};
